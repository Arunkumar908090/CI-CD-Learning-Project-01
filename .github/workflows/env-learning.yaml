name: Env's and Variables leraning side

on:
    push:

jobs:
    build:
        runs-on: ubuntu-latest
        
        steps:

          - name: Script for Checking Machine
            run: echo "My Machine runs successfully!"

          - name: Env setup
            env:
                APP_ID: Demo App
            run: echo "My $APP_ID runs successfully!"